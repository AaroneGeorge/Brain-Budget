● I've finished the research — pulled the hackathon page, the 1Shot co-host blog post, three previous editions of
  this same "Dev Cook-Off" series, and the underlying tech (ERC-7710/7715, x402, Venice's API). Here's the
  analysis, leading with the thing that matters most:

  ⚠️  First, the urgent part: you have ~4 days
  
  The submission deadline is June 15, 2026, 10:59 — today is June 11. Registration is open until the deadline, so
  you can still enter, but this completely changes the strategy: this is now a "scope ruthlessly, demo-video-first"
  sprint, not a build-your-dream-app project. Everything below is filtered through that lens.

  The basics (30 seconds)

  $14,000 pool, online, hosted by MetaMask Developer with 1Shot API and Venice AI. Seven tracks: x402 + ERC-7710
  ($3k), Best Agent ($3k), A2A Coordination ($3k), Best use of Venice AI ($3k), 1Shot Permissionless Relayer ($1k
  USDC, mainnet required), plus Best Feedback and Best Social Media ($500 each, 5×$100). Every submission must
  integrate MetaMask Smart Accounts Kit (or ERC-7715 Advanced Permissions) in the main application flow, with a
  demo video showing it working.

  The non-obvious stuff

  1. Prize stacking is designed into the rules. The Venice track explicitly requires that your project "must
  qualify for the main tracks" — meaning one project is expected to enter multiple tracks. A single
  well-architected build can plausibly take x402+7710 ($1,500) + Venice ($1,500) + 1Shot Relayer ($500) + Feedback
  ($100) + Social ($100) ≈ $3,700 from one codebase. Most hackers build for one track. Don't.

  2. The real competition is a fraction of "228+ participants." I checked three previous editions of this exact
  series: the Advanced Permissions edition had 163+ registrants, DTK had 161+, Monad had 391+. The Monad edition's
  winner roundup shows the same handful of projects winning across tracks — typical Web3 hackathon
  registration→qualifying-submission conversion is 10–20%, and the Smart Accounts Kit integration bar filters out
  most of those. With 24 separate prize slots and the rule that "judges may withhold prizes if submissions don't
  meet quality standards" (meaning some tracks may literally go unclaimed), a compliant, working entry has
  unusually high expected value. The bar to beat isn't 228 people — it's maybe 5–15 serious projects per track.

  3. The grading rubric is leaked in the 1Shot blog post. Their co-host announcement
  (https://1shotapi.com/blog/metamask-1shot-api-dev-cook-off) says outright that for the relayer track,
  "webhook-based status monitoring scores higher," that gas can be paid in stablecoins (USDC/USDT/USDG/MUSD), and
  that judges "won't award prizes for cosmetic implementations" — permissions must be core functionality, "not
  peripheral features." Each track has a specific assigned judge, and the sponsor tracks are judged by that
  sponsor's own DevRel. The two workshop recordings (1Shot API on May 26, x402+7710 on May 28) are effectively the
  judges telling you what they want to see — watch them at 2x before writing a line of code.

  4. x402 + ERC-7710 has almost no prior art — this is a "reference implementation wins" track. x402 is Coinbase's
  HTTP-402 agentic payment standard; ConsenSys only recently committed to supporting it
  (https://metamask.io/news/self-custody-in-the-era-of-agents). Combining it with ERC-7710 delegation redemption is
  bleeding-edge enough that there are essentially zero public examples. The first clean, well-documented
  implementation of "agent pays per-request via x402 using delegated authority" doesn't have to be a great product
  — it has to exist and work. Same dynamic, even stronger, for A2A Coordination: it requires redelegation
  (Alice→Bob→Carol delegation chains with narrowing caveats), which is the least-understood corner of the Smart
  Accounts Kit (https://docs.metamask.io/smart-accounts-kit/concepts/delegation/). Caveat enforcers are the
  technical moat here — a custom Solidity caveat enforcer (e.g., "this sub-agent can only spend during market
  hours" or "only on whitelisted contracts") is exactly the kind of thing MetaMask DevRel judges geek out over,
  because it showcases their framework's composability better than they usually manage themselves.

  5. There's a hidden friction trap in ERC-7715. Advanced Permissions (the wallet_grantPermissions flow through the
  MetaMask extension) has historically required MetaMask Flask (the developer build) and is still experimental.
  The rules' "signer agnostic" language — compatible with the extension, Embedded Wallets, Dynamic, or Privy — is a
  quiet hint: you can sidestep Flask entirely by using embedded/headless smart accounts from the Kit, creating and
  redeeming 7710 delegations programmatically. For an agent demo built in 4 days, headless is dramatically less
  likely to break during your demo recording. Many competitors will burn days fighting Flask.

  6. The three sponsors compose into one narrative — and that narrative is the winning demo. Venice's API accepts
  pay-per-request USDC on Base from any wallet, alongside its VVV-staking inference model. That means the maximally
  judge-pleasing story is circular and self-demonstrating: a user delegates a scoped budget to an AI agent
  (MetaMask 7710, with caveats) → the agent autonomously pays for its own Venice inference per-request in USDC
  (x402-style flow) → transactions relayed gaslessly through 1Shot's permissionless relayer (7702 authorization,
  webhook monitoring shown on screen). An agent that pays for its own brain with delegated funds hits all four paid
  tracks with one demo. Venice's privacy/no-data-retention angle ("the agent's reasoning never leaves a
  privacy-preserving inference layer") is also a differentiator nobody else will articulate — check whether
  Venice's USDC payment endpoint is exposed via actual x402 headers; if so, the integration is nearly free.

  7. The 1Shot relayer track is small money but nearly uncontested. It requires relaying real 7710 transactions
  through their mainnet relayer with 7702 authorizations — i.e., spending actual money on gas/stablecoins. That
  friction means very few entries for 2×$500 slots. If your project already produces 7710 redemptions, routing them
  through 1Shot is incremental work, and "no signup, JSON-RPC native" means it's genuinely a config-level change.

  8. The $1,000 in "soft" tracks is the highest $/hour in the event. Best Feedback (5×$100) rewards detailed bug
  reports and DX critiques of the Smart Accounts Kit — which you'll generate naturally while building; just write
  them down as you hit them. Best Social Media (5×$100) is build-in-public posts on X. Beyond the cash, both put
  your name in front of the track judges before they score your project — these mini-tracks double as
  judge-attention arbitrage.

  9. This series is a pipeline, not a one-off. MetaMask has run these Dev Cook-Offs roughly monthly (DTK → Advanced
  Permissions → Monad → this one), with overlapping DevRel judges. Past winners got sponsor blog coverage (Envio
  wrote up the Monad winners (https://docs.envio.dev/blog/metamask-smart-accounts-hackathon-winners)) and follow-on
  relationships with the ConsenSys ecosystem, which is actively funding agentic-wallet work right now. Even a
  2nd/3rd place here is a credential for the next edition and for MetaMask's grants/Linea ecosystem pipeline.

  What I'd do with 4 days

  Build one thin agent around the circular narrative in point 6, scoped to a single user flow. Day 1: watch both
  workshop recordings, scaffold headless smart accounts + a 7710 delegation with one custom caveat. Day 2: wire
  Venice inference + the x402/USDC payment loop. Day 3: route redemptions through 1Shot's mainnet relayer with
  webhook status displayed in the UI, and post build-in-public updates. Day 4: nothing but the demo video, README,
  and feedback write-up — judging is video-first, and "judges may withhold prizes" cuts both ways: a
  clearly-working, clearly-explained demo beats a more ambitious half-working one in this format, every time.

  Sources: Hackathon page
  (https://www.hackquest.io/hackathons/MetaMask-Smart-Accounts-Kit-x-1Shot-API-x-Venice-AI-Dev-Cook-Off) · 1Shot
  co-host blog (https://1shotapi.com/blog/metamask-1shot-api-dev-cook-off) · Smart Accounts Kit docs
  (https://docs.metamask.io/smart-accounts-kit/) · Redelegation guide
  (https://docs.metamask.io/smart-accounts-kit/development/guides/delegation/create-redelegation/) · ERC-7710
  redeem delegations (https://docs.metamask.io/smart-accounts-kit/0.13.0/experimental/erc-7710-redeem-delegations/)
  · MetaMask on x402/agents (https://metamask.io/news/self-custody-in-the-era-of-agents) · Venice API docs
  (https://docs.venice.ai/overview/about-venice) · VVV token (https://venice.ai/token) · Monad edition winners
  (https://docs.envio.dev/blog/metamask-smart-accounts-hackathon-winners) · Advanced Permissions edition
  (https://www.hackquest.io/hackathons/MetaMask-Advanced-Permissions-Dev-Cook-Off) · DTK edition
  (https://www.hackquest.io/hackathons/MetaMask-Delegation-Toolkit-DTK-Dev-Cook-Off)

  If you want, I can scaffold the project right now — the repo is empty and 4 days is tight.
